# User Profiles

## Building the server:

To build and run the server, run these commands:
```bash
cd server
npm start
```

## Building the UI:

To build and run the UI:
```bash
cd client
npm start
```
